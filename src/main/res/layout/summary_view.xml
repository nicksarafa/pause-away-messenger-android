<?xml version="1.0" encoding="utf-8"?>
<com.pauselabs.pause.view.tabs.SummaryView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/home_summary"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/transparent_drawable">

    <!-- Fab -->
    <RelativeLayout
        android:id="@+id/fab_container"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/emoji_summary_sothree_height"
        android:background="@drawable/transparent_drawable">

        <ImageButton
            android:id="@+id/fab_image_button"
            android:layout_width="@dimen/fab_button_diameter"
            android:layout_height="@dimen/fab_button_diameter"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="@dimen/fab_button_margin"
            android:layout_marginBottom="@dimen/fab_button_margin"
            android:layout_marginRight="@dimen/fab_button_margin"
            android:layout_marginEnd="@dimen/fab_button_margin"
            android:background="@drawable/fab_button"
            android:src="@drawable/ic_action_sleep"
            android:tint="@android:color/white"
            android:contentDescription="Start Paüse Session" />

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/summary_view_container"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/fab_container"
        android:background="@color/pause_dark_grey">

        <ListView
            android:id="@+id/summary_card_list_view"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:listSelector="@android:color/transparent"
            android:cacheColorHint="@android:color/transparent"/>

        <TextView
            android:id="@+id/summary_no_messages"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:textSize="36sp"
            android:textColor="@color/pause_green"
            android:text="No Messages"
            />

    </RelativeLayout>

</com.pauselabs.pause.view.tabs.SummaryView>