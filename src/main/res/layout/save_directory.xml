<?xml version="1.0" encoding="utf-8"?>

<com.pauselabs.pause.view.tabs.ASCIIDirectoryView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/saveDirectory"
    android:layout_height="fill_parent"
    android:layout_width="fill_parent"
    android:background="@drawable/transparent_drawable">

    <ListView
        android:id="@+id/saveDirectoryList"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:numColumns="1"
        android:layout_marginTop="@dimen/list_view_universal_margin_top"
        android:listSelector="@drawable/transparent_drawable"
        android:cacheColorHint="@color/transparent"
        android:clipToPadding="true"
        android:scrollbars="vertical"
        android:isScrollContainer="false"
        android:scrollbarStyle="outsideOverlay"
        android:smoothScrollbar="true"
        android:verticalScrollbarPosition="left"
        android:overScrollMode="never"
        android:overScrollHeader="@drawable/transparent_drawable"
        android:headerDividersEnabled="false"
        android:overScrollFooter="@android:color/transparent"
        android:footerDividersEnabled="false"
        android:stretchMode="columnWidth"/>

        <!-- Custom Message View Holder -->

        <RelativeLayout
            android:id="@+id/custom_pause_container"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="@dimen/view_header_universal_height"
            android:layout_alignParentTop="true"
            android:layout_margin="8dp"
            android:layout_alignTop="@id/saveDirectoryList"
            android:background="@drawable/card_bg">

            <EditText
                android:id="@+id/custom_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/transparent_drawable"
                android:layout_centerInParent="true"
                android:focusableInTouchMode="true"
                android:focusable="true"
                android:textSize="@dimen/text_medium"
                android:textColorHint="@color/text"
                android:textColor="@color/text"
                android:textIsSelectable="false"
                android:textCursorDrawable="@drawable/cursor"
                android:freezesText="false"
                android:maxLines="10"
                android:scrollbars="none"
                android:imeOptions="flagNoEnterAction"
                android:layout_alignParentLeft="true"
                android:layout_alignParentStart="true"
                android:paddingLeft="@dimen/save_horizontal_margin"
                android:paddingRight="@dimen/save_horizontal_margin"
                android:inputType="textMultiLine"
                android:hint="@string/custom_pause_hint"/>

        </RelativeLayout>

</com.pauselabs.pause.view.tabs.ASCIIDirectoryView>

